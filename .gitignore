# API keys — never commit
.env
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Virtual environment
venv/

# OS
.DS_Store
Thumbs.db

# Large raw data downloads (re-downloadable, not needed at runtime)
data/raw/FARS2022NationalCSV.zip

# NOTE: db/terrain.db is intentionally NOT ignored.
# Streamlit Community Cloud cannot run the pipeline scripts at deploy time
# (they require local API keys). The pre-built database must be committed.

# NOTE: data/raw/counties_geojson.json is intentionally NOT ignored.
# The app reads it at runtime for the choropleth map.
